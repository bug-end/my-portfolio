import { Rubik_Broken_Fax, Space_Grotesk } from 'next/font/google';

export const space_grotesk = Space_Grotesk({
  subsets: ['latin-ext'],
  display: 'swap',
});

export const rubik_glitch = Rubik_Broken_Fax({
  weight: ['400'],
  display: 'swap',
});
